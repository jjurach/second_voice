# Change Documentation: Fixed Groq URL and Added Debug Flags

## Date: 2026-01-25 02:35:00

## Description
Fixed the Groq API endpoint to use the correct OpenAI-compatible URL for Whisper transcriptions. Added `--debug` and `--verbose` CLI flags as requested.

## Changes

### CLI
- **`src/cli/run.py`**:
    - Added `--debug` and `--verbose` arguments.
    - Updated `ConfigurationManager` to accept these flags.

### Core Processing
- **`src/second_voice/core/processor.py`**:
    - Updated `_transcribe_groq` to use `https://api.groq.com/openai/v1/audio/transcriptions`.
    - Switched to `multipart/form-data` upload (using `files` parameter).
    - Added `model='whisper-large-v3'` parameter required by the API.

## Verification Results
- Ran `python3 src/cli/run.py --debug --verbose --mode menu --file samples/test.wav`.
- CLI arguments were accepted.
- Transcription succeeded (Groq API responded correctly).
